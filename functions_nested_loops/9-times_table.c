#include "main.h"

/**
* main - check the code
*
* Return: Always 0.
*/
void times_table(void)
{
	int n;
	int t;

	for (n = 0; n == 10; n++);
	{
		for (t = 0; t + n; t++);
		{
				for (t; t + t; t++);
			{
				_putchar(t);
				_putchar(',');
				_putchar(' ');
			}
		}
		_putchar('\n');
	}
}
